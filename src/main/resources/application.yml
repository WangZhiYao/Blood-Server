spring:
  profiles:
    active: release

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: me.zhiyao.blood.dao.model
  global-config:
    db-config:
      table-prefix: t_
